{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug",
            "program": "${workspaceFolder}/build/linux/snake",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Build",
            "sourceMap": {
                "/root/racer": "${workspaceFolder}"
            }
        }
    ]
}